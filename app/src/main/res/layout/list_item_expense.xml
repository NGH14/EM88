<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	android:id="@+id/layout_list_item_expense"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:orientation="vertical"
	android:padding="10dp"
	tools:ignore="HardcodedText">

	<LinearLayout
		android:layout_width="match_parent"
		android:layout_height="35dp">

		<TextView
			android:id="@+id/list_item_expense_type"
			android:layout_width="0dp"
			android:layout_height="match_parent"
			android:layout_weight="1"
			android:gravity="center_vertical"
			android:text="type"
			android:textSize="18sp"
			android:textStyle="bold" />

		<TextView
			android:id="@+id/list_item_expense_amount"
			android:layout_width="0dp"
			android:layout_height="match_parent"
			android:layout_weight="1"
			android:drawablePadding="5dp"
			android:gravity="center_vertical|end"
			android:text="amount"
			android:textSize="15sp"
			app:drawableEndCompat="@drawable/ic_dollar_sign"
			app:drawableTint="?android:textColor" />
	</LinearLayout>

	<TextView
		android:id="@+id/list_item_expense_date_spent"
		android:layout_width="match_parent"
		android:layout_height="30dp"
		android:gravity="center_vertical"
		android:text="date time"
		android:textSize="15sp" />
</LinearLayout>
